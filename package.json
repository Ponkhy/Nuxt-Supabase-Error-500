{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "HOST=127.0.0.1 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/supabase": "^0.2.1",
    "nuxt": "3.0.0-rc.12"
  }
}
